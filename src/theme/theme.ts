import { COLORS } from './colors'
import { FONT_FAMILIES, FONT_SIZES, FONT_WEIGHTS } from './fonts'

export const THEME = {
    colors: COLORS,
    fontWeights: FONT_WEIGHTS,
    fontSizes: FONT_SIZES,
    fontFamilies: FONT_FAMILIES,
} as const
